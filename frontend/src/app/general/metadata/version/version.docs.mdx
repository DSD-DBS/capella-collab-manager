{/*
    SPDX-FileCopyrightText: Copyright DB InfraGO AG and contributors
    SPDX-License-Identifier: Apache-2.0
*/}

import * as Component from './version.stories.ts'
import { Meta, Title, Story, Canvas, Unstyled } from '@storybook/blocks'

<Meta of={Component} />

<Title />

When the version is unchanged, it looks like this:

<Story of={Component.VersionUnchanged} />

When the version is changed and the user has used the app before, it looks like this:

<Story of={Component.NewVersionAvailable} />

If the user has never used the app before, it looks like this:

<Story of={Component.FirstTimeAccessed} />
