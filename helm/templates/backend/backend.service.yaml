apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
  labels: 
    id: {{ .Release.Name }}-service-backend
spec:
  type: ClusterIP
  selector:
    id: {{ .Release.Name }}-deployment-backend
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
