{{ if eq .Values.target "local" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-licence-server-mock
  labels: 
    id: {{ .Release.Name }}-service-licence-server-mock
spec:
  type: ClusterIP
  selector:
    id: {{ .Release.Name }}-deployment-licence-server-mock
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
{{ end }}
